\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/qualitative/monuseg_main_grid}
  \caption{\textbf{Qualitative comparison on MoNuSeg (test split).}
    Each row shows one sample. Columns: Input image, Ground truth, A0 (FPN-2 only),
    A2 (coarse+fine), A3 (memory), \textbf{Ours} (staged final), Error map
    (green = TP, red = FP, blue = FN).}
  \label{fig:monuseg_qualitative}
\end{figure}
