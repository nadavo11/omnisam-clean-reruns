\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/qualitative/glas_main_grid}
  \caption{\textbf{Qualitative comparison on GlaS (test split).}
    Each row shows one sample. Columns: Input image, Ground truth, A0 (FPN-2 only),
    A2 (coarse+fine), A3 (memory), Error map
    (green = TP, red = FP, blue = FN).}
  \label{fig:glas_qualitative}
\end{figure}
